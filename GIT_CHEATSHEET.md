# Памятка по основным командам Git

## 🟢 Настройка Git
```
git config --global user.name "Твоё Имя"
git config --global user.email "youremail@example.com"
git config --global core.autocrlf true   # Для Windows (автоматическая конвертация концов строк)
```

## 🟢 Работа с репозиторием
```
git init                   # Инициализировать новый репозиторий в текущей папке
git clone <url>            # Клонировать удалённый репозиторий
git status                 # Посмотреть состояние файлов (что изменено, что готово к коммиту)
git log                    # История коммитов
git diff                   # Посмотреть отличия (что изменено)
```

## 🟢 Работа с файлами
```
git add <file>             # Добавить файл в индекс (staging area)
git add .                  # Добавить все изменения в текущей папке
git restore <file>         # Отменить изменения в файле (до последнего коммита)
git restore --staged <file> # Убрать файл из индекса (оставить изменения в рабочей копии)
```

## 🟢 Коммиты
```
git commit -m "Комментарий"    # Сделать коммит с сообщением
git commit --amend             # Изменить последний коммит (если не было push)
```

## 🟢 Работа с удалённым репозиторием
```
git remote -v                  # Посмотреть список подключённых удалённых репозиториев
git push                       # Отправить коммиты на сервер (GitHub)
git pull                       # Забрать изменения с сервера
git fetch                      # Получить изменения с сервера (без слияния)
```

## 🟢 Ветвление
```
git branch                     # Посмотреть список веток
git branch <name>              # Создать новую ветку
git checkout <name>            # Переключиться на ветку
git checkout -b <name>         # Создать и сразу переключиться на новую ветку
git merge <name>               # Слить ветку <name> в текущую
```

## 🟢 Сброс и отмена
```
git reset HEAD <file>          # Убрать файл из индекса (оставить изменения)
git reset --soft HEAD~1        # Откатить последний коммит, оставив изменения в рабочей копии
git reset --hard HEAD~1        # Откатить последний коммит и удалить изменения (ОСТОРОЖНО!)
```

## 🟢 Просмотр и поиск
```
git show <commit>              # Посмотреть детали коммита
git blame <file>               # Посмотреть, кто и когда изменял каждую строку файла
git log --oneline --graph      # Краткая история с визуализацией ветвлений
```

## 🟢 Справка
```
git help <команда>             # Получить справку по команде
``` 